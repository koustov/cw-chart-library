import React from 'react'
import { MultiVariantChart, MultiVariantChartProps } from '..'

/**
 * Primary UI component for user interaction
 */
export const MultiVariantChartStoryElement = (props: MultiVariantChartProps) => {
  return <MultiVariantChart {...props} />
}
